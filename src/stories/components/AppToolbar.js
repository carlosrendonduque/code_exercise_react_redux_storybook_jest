import React from 'react';

import AppToolbar from '../../components/AppToolbar';


export default {
  component: AppToolbar,
  title: 'Design System/Components/AppToolbar',
};

const Template = args => <AppToolbar {...args} />;

export const Default = Template.bind({});

